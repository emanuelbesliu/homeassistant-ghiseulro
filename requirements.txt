# Ghiseul.ro Home Assistant Integration Dependencies
# These dependencies must be kept in sync with manifest.json

# HTTP client (used with Cloudflare clearance cookies from FlareSolverr)
requests>=2.31.0
