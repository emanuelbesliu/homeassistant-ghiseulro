---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ghiseul-browser
  namespace: flaresolverr
  labels:
    app: ghiseul-browser
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ghiseul-browser
  template:
    metadata:
      labels:
        app: ghiseul-browser
    spec:
      containers:
        - name: ghiseul-browser
          image: ghcr.io/emanuelbesliu/ghiseul-browser:v0.1.8
          ports:
            - containerPort: 8192
              protocol: TCP
          env:
            - name: LOG_LEVEL
              value: info
            - name: HEADLESS
              value: "true"
            - name: TZ
              value: Europe/Bucharest
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "1"
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /health
              port: 8192
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8192
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: ghiseul-browser
  namespace: flaresolverr
  labels:
    app: ghiseul-browser
spec:
  type: LoadBalancer
  selector:
    app: ghiseul-browser
  ports:
    - port: 8192
      targetPort: 8192
      protocol: TCP
      name: http
